# 项目采集与实时搜索规则

## 核心目标

- 优先提高可验证、相关、去重后的有效数据量，同时保证搜索结果的实时性。
- 优化必须面向从零开始的新一轮采集，核心指标是单位时间内新增的唯一有效正文数量；不得把挖掘、修复或重新处理历史采集数据作为主要增量路径。
- 历史实验和失败样本只用于建立基线、发现瓶颈和验收新采集效果，不能计入新一轮采集增量。
- 需要通过多个新的独立短时实验，尝试并比较查询供给、调度、并发、抓取和上传方法，以新采集实测结果决定保留或回退改动。
- 默认不启动无人值守的长时间持续采集；除非用户明确要求，只运行有固定时长、单一变量和停止条件的对照实验。
- 最终必须回答两个问题：哪种方法能最大化新增唯一有效正文，以及在统一实验口径下实际最多能采集多少。
- 优化过程必须持续覆盖三类实验：固定 10 分钟的快速方法对照、固定数小时的吞吐与稳定性验证、以及大量用户同时查询同一关键词的合并复用压测。
- 三类实验必须使用独立账本或隔离的压测记录，明确固定窗口、并发规模、基线与停止条件；不得把请求数或重复响应当作新增正文。
- 所有相关设计与优化都必须兼顾完整覆盖、响应速度、系统稳定性和外部服务限制。

## 生产验收与外部数据商对比

- 生产可用最低按完整 24 小时 Whale accepted **100,000 条/天**（4,167 条/小时）；当前中文日量冲刺目标为 **1,000,000 条/天**（41,667 条/小时）。
- 只把固定窗口内 `classification=new`、`quality=[]`、历史 URL/正文指纹去重、带明确时区 `published_at`、Whale `accepted/queued` 回执都满足的数据算生产成果；正文数、搜索请求数、返回链接数、供应商 credits 都不能直接当作产量。
- 已观察单 IP 首小时爆发约 271–286 条有效正文/小时，但完整 2 小时稳定值约 89 条/小时，长时间冷却后会更低；不能用首小时峰值线性放大。
- 外部数据服务商的公开参数只作比较，必须在本项目同口径下抽样验收后再接入。

| 供应商 | 公开宣称速度/量 | 只能证明什么 |
| --- | --- | --- |
| Serper | 1–2 秒；Starter 50 QPS，Ultimate 300 QPS；按 credits 计费 | 返回 Google SERP 的速度和请求配额，不证明最终合格正文量 |
| Bright Data SERP | 宣称 <1 秒，免费 5k 请求/月，按成功请求计费 | 提供多引擎 SERP 请求能力，不证明正文质量和去重 |
| DataForSEO SERP | 宣称实时，周转最长约 6 秒，提供 MCP | 提供结构化 SERP，不证明原站正文可抓或发布时间完整 |
| Apify Google Search Scraper | 约 $1.80/1,000 个结果页 | 提供托管抓取任务，不直接等于 Whale accepted |

- 任何外部数据服务商接入前，必须用独立校验任务验证：同一关键词前 1–11 页覆盖、真实 SERP 外链数、URL/正文去重、正文质量、发布时间可解析率、Whale accepted 回执和下游可见性；禁止用供应商的“请求数、credits、搜索次数”推算生产产能。

## 前 11 页完整覆盖

- 每个用户关键词都必须持续尝试搜索第 1–11 页，不得因为新增链接较少而提前结束。
- 分页任务必须持久化页码、状态、候选数量、唯一 URL 数量、失败原因和重试时间。
- 搜索失败、进程重启或服务冷却后，必须从未完成页继续，不得丢失或重复推进进度。
- 明确无结果的页面可以标记完成；验证码、限流、超时、网络失败或解析异常不得视为空结果。
- 搜索结果进入正文采集前必须执行 URL 去重；正文保存前必须执行内容指纹去重。

## 高并发实时搜索

- 大量用户并发测试只压测本项目 API，由系统统一调度对外搜索请求；虚拟用户不得直接并发冲击第三方搜索服务。
- 用户请求应优先快速返回首批结果和实时进度，其余页面在后台异步完成，不能等待全部 11 页后才响应。
- 相同关键词、语言和页码的并发请求必须合并并复用结果，避免重复搜索和资源浪费。
- 搜索发现、正文抓取、数据处理和上传必须解耦，通过持久队列、租约和幂等键支持水平扩展与故障恢复。
- 并发必须自适应调整：系统健康时逐级升高；出现限流、验证码、错误率升高或队列积压时立即降载并按退避策略恢复。
- 必须设置全局、单关键词、单域名和单出口并发限制，并保持公平调度，避免单个关键词或站点占满资源。

## 数据质量与实时性

- 优化目标以新增的唯一有效正文为准，不得仅以请求数、发现链接数或抓取次数衡量产出。
- 缓存必须有明确的 TTL；实时请求优先复用仍满足新鲜度要求的结果，过期结果应重新搜索。
- 不得为了提高数量而降低相关性、正文完整性、发布时间真实性或去重标准。
- 不得绕过验证码、登录、付费墙或其他访问控制；限流与封禁信号必须进入冷却和熔断流程。

## 验收与监控

- 每次相关改动都必须验证：前 11 页覆盖率、唯一有效数据量、吞吐量、P50/P95 延迟、错误率、限流率和恢复能力。
- 高并发测试必须包含不同关键词并发、相同关键词并发、突发流量、持续流量、第三方限流和服务重启场景。
- 新增或修改调度、分页、重试、去重、缓存及并发逻辑时，必须补充自动化测试。
- 性能提升不得以数据丢失、重复写入、无限重试、跳过失败页或关闭安全保护为代价。
