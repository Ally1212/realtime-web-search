# Google 日增百万条持续优化任务

> 最新执行安排：用户要求当前 v5 持续运行至 2026-09-16 09:00（UTC+8），已在线缩短截止时间，不重启、不清零；09:00 停止新任务，最多约两分钟收尾。起点仍为 2026-09-15 13:28:13，总观察窗口约 19 小时 32 分钟，不作为完整 24 小时验收。截止修改审计见 `research/million-v5-user-stop-20260916-0900.json`。


目标保持进行中，只有连续完整 24 小时内取得至少 1,000,000 条历史去重、相关、正文有效、带有效发布时间且被 Whale 新增接收的数据才算达标。每天百万对应每分钟 694.44 条、每秒 11.574 条；不将正文数、重复回执或短时外推计入。发现限定 Google Web，查询语言 zh，正文继续按现有自动质量规则验收，不增加 News、CSE 或其他搜索来源。

当前可复核基线为两小时新增接收 6,724 条，平均每分钟 56.03 条。达标需要约 12.4 倍的平均接收速度提升。上轮 82.4% 接收来自站点查询；4,482 条有效正文因日期缺失未上传，33.5 分钟被记为搜索冷却，另有 1,194 次重复成功查询页。

## 第一轮：yield v2

- 纳入已验证的两项调度修复：备用通道冷却不误停健康通道；成功查询页 24 小时后刷新，失败页独立重试。
- 搜索预算按 80% site、10% recent、5% topic、5% event 分配。暂不以线程增加等同请求速率增加，继续执行共享 Google 配额、出口冷却和每域正文并发限制。
- 从历史 Whale 接收记录选最多 160 个至少接收过 3 条的站点，扩展五组 AI 主题、最近八个完整年份；前 60 个站点再扩展最近 24 个完整月份。新增时间分片初始设置三页，优先独立查询覆盖；日期运算符不作为原站发布时间证据。
- 正文 24 个常驻进程，搜索 4 线程，独立上传，使用 Docker 原生卷与只读源码快照。
- 导入上轮及既有实验的有效 URL/hash 基线，防止用已采数据重新充数。新建 24 小时实验，按阶段观察每次请求的实际接收量、日期损耗、重复率及资源占用。

## 持续迭代

根据实测逐项处理：查询边际新产出和时间分片密度；明确发布时间的解析性能与覆盖；正文进程内存及 CPU；健康出口的有效吞吐和搜索并发；账本与上传持续吞吐。放大并发前先确认瓶颈，候选版本使用独立快照和实验账本，不把不同版本的短测包装为单版本 24 小时验收。

运行故障应修复后恢复或新建可审计实验；模型目标保持 active。达到百万条之前不标记任务完成，也不因一次未达标而结束迭代。若后续需要新增付费算力或代理，先形成可核对的容量差额与成本方案；当前先使用已配置资源。

## 当前验证

新增查询分片和加权调度回归通过。全套本机测试共 120 项：109 项通过，11 项需要数据库的测试跳过。上轮正式快照曾在独立 Docker 测试数据库完成 113 项全部通过；本轮新增逻辑尚需运行数据验证，不能沿用上轮吞吐结论。

持续运行记录保存于 `docs/research/million-campaign-progress.jsonl`，每轮源码清单保存为对应实验 key 的 `-code.json`。完整 24 小时最终验收按 SQLite 账本中的正文完成时间、上传回执时间、质量及历史去重状态复核。

## 第二轮：v3 候选与验证

- 选择性解析 JSON-LD 和日期节点，保留原正文/日期质量语义。519 KB 合成页面局部测试输出一致，JSON-LD 解析约快 2 倍、日期约快 4.8 倍；这不是端到端速度结论。基准：`research/million-v3-parser-microbenchmark.json`。
- 共享搜索上限可显式配置，默认仍 2 RPS；下一轮使用 4 RPS、12 搜索线程、32 正文进程。继续执行 PostgreSQL 共享预留、全局熔断、出口冷却与每站点正文最多 2 并发。
- 持久化查询供给：最多 160 个已验证站点 × 25 个分散 AI 主题 × 120 个完整月份，最多 480,000 组候选查询；实际上限由历史合格站点数决定。未访问站点查询不足 5,000 时，每分钟最多追加 2,000 组，游标与查询原子提交，重启不重置已完成页。扩展量不等于实际可发现或可接收量。
- 调整查询调度排序，使已建立的索引可提前终止。10 万查询/30 万页面合成基准：118 ms → 0.031 ms/次，首个任务一致，公平调度回归通过。基准：`research/million-v3-scheduler-microbenchmark.json`。
- 搜索统计改成逐条累加，避免一次创建全部历史请求的 Python 列表。存储预算可配置，默认仍 10 GiB；本轮使用 64 GiB，并保留至少 2 GiB 可用空间限制。修复嵌套导出目录被重复计入的问题。

本机回归 127 项，114 通过、13 数据库测试跳过；Docker 独立数据库完整回归 127 项全部通过，测试数据库已删除。测试不连接真实 Whale 上传端点。正式运行前暂停并排空 v2，将其有效 URL/hash 纳入 v3 历史基线；v2 的短阶段不能作为完整 24 小时结果。

### v3 正式启动

v2 为迭代而中止，状态 paused，累计有效新正文 6,166、Whale 新增接收 5,446、缺日期 720、pending 0；并未完成 24 小时。已纳入 v3 基线。

正在运行 `million-yield-v3-20260915`，32 正文/12 搜索/共享 4 RPS/64 GiB 账本预算，独立只读源码快照；固定起止时间与 v2 最终统计见 `research/million-v2-to-v3-transition.json`。目标保持 active，尚未达标。

## 第三轮：v4 修复实际供给瓶颈

v3 的 4 RPS 配置未带来相应网络请求量。调查确认当时代理缓存有 160 个新鲜端点配置（后续确认只对应 80 台主机；端点数量和数据库历史记录都不能作为独立出口容量）。一次采样中 30 个验证码冷却、83 个因为 `google_unrecognized_page` 处于 5 分钟冷却，仅 47 个在就绪或 30 秒间隔中。4 RPS 健康恢复后，一分钟出现 141 次 `google_proxy_unavailable`，实际请求约 100 次。

三个失败查询的独立诊断仍返回 HTTP 200 的 Google 导航页，没有结果节点，也没有明确“无结果”文案。证据保存在 `research/million-v3-diagnostic-serp/`。诊断发出 3 次额外搜索，使用相同共享配额与出口预留；不计入正式实验采集量。

v4 修改：

- HTTP 200 且仅解析失败时，出口冷却从 300 秒改为至少 30 秒，仍保留失败计数、禁止缓存为空结果、原有全局熔断与每出口最低间隔。验证码/403/429 冷却不变；超时和未知 HTTP 状态继续 300 秒。这只调整对出口故障的归因，绝不将导航页视为有效数据。
- 启动时继承历史实验最近 24 小时成功查询页的刷新时间；失败、过期和将来的记录不继承。减少换版本时重复搜索。
- 正文进程完成 200 个任务或完成任务后的 RSS 达到 192 MiB 时回收并按需替换；已经返回的结果先入完成队列，不丢失或重复上传。这是任务之间的回收阈值，不能作为单次解析的硬内存上限。
- 为可抓取 URL 建立局部索引，排除进行中、历史跳过和未发生变化的行；分钟采样省略昂贵的按来源关联统计，完整报告仍保留。10 万 URL 的局部索引探索基准见 `research/million-v4-url-scheduler-microbenchmark.json`，不等于整体吞吐提升。
- 每分钟检查代理缓存到期；实际 API 同步仍由原有 1,800 秒间隔控制，避免启动时跳过同步后额外延后半小时。

完整 Docker 独立数据库回归 131 项全部通过，临时数据库已删除；本机同 131 项中 118 通过、13 数据库项跳过（随后仅调整回收默认阈值/缓存轮询，并在 Docker 全套重新验证）。`git diff --check` 通过。v4 使用独立快照重新启动 24 小时窗口，纳入 v3/v2/既有实验历史基线，不拼接短阶段声称百万达标。

### v4 正式启动

`million-yield-v4-20260915` 于 2026-09-15 13:16:55（UTC+8）启动，固定截止 2026-09-16 13:16:55。29 个源码快照文件 hash 校验通过；继承 5,029 个近期成功查询页的刷新时间，历史基线为 52,809 个 URL 与 51,311 个正文指纹。分钟监控脚本 `scripts/watch_million.py` 独立运行，记录到 `research/million-yield-v4-20260915-monitor.log` 和统一进度 JSONL。

v3 因已定位的供给瓶颈而中止，最终有效新正文 1,309、Whale 新增接收 1,137、缺日期 172、pending 0；并非 24 小时成绩。v3 完整阶段数据和 v4 固定窗口见 `research/million-v3-to-v4-transition.json`。当前目标保持 active，尚未达标。

#### v4 启动后的调度记录修正

13:20:01（UTC+8）发现仅继承成功搜索记录会让旧失败查询获得“未访问”的优先级，启动阶段站点查询 116 页仅返回 23 个结果。已通过 `scripts/repair_experiment_frontier.py` 将 481 个历史查询的 last_served 恢复为最近尝试时间；不改源码快照、页面 due、质量/去重/回执、开始或截止时间。操作在单一事务中执行，更新条件禁止覆盖更新的本轮调度时间。审计：`research/million-yield-v4-20260915-frontier-repair.json`。

因此该轮包含一次明确记录的运行中队列优先级修正，并非队列参数全程静态的对照实验。工作区同时修复了后续启动逻辑，相关 21 项流水线测试通过；此后工作区的启动修复尚未替换正在运行的 v4 源码快照。

## 第四轮：v5 正文容量与连续调参

v4 已实测每分钟约 238–240 次 Google 通道请求、0 验证码，但最近分钟接收约 604–672 条，正文 32 个槽位满载，待抓队列从约 250 增至 795 并继续上升。约 10 分钟累计接收 4,788 条；启动阶段的旧失败查询优先级问题已如实记录，不能把该累计均速和稳定区间混用。Docker 提供 11 CPU、7.75 GiB 内存，单采集容器此时约 4.82 GiB、2.35 核，仍有 CPU 余量。

v5 保留 Google 4 RPS/12 搜索线程，正文先增加至 40 个进程，每任务完成后 RSS 回收阈值降低为 128 MiB。与 200 任务回收同时生效；内存阈值仍不是解析过程中的硬限制。每分钟重新读取正文并发与回收阈值，修改会写入带时间的 settings 审计项；缩容只回收空闲进程，进行中任务完成后再回收，不丢弃结果。允许在同一固定 24 小时窗口内调整正文并发并保留审计，不改开始/截止时间，也不需要热替换运行源码。

启动进度继承同时纳入失败查询的 last_served，避免重复 v4 启动问题。新审计脚本 `scripts/audit_million.py` 只计入正文与 Whale accepted 回执均落在固定窗口内的有效 new；额外检查历史基线重合、本轮重复 URL/hash，并要求完整 24 小时正常截止。accepted/queued 仍仅代表 Whale 接收回执，没有额外验证后续索引完成；内容质量为现有自动规则验收。

本机全套 134 项：121 通过、13 数据库测试跳过。Docker 独立数据库 134 项全部通过，临时数据库已删除；`git diff --check` 通过。该验证包括缩容时保留进行中任务、并发变更审计、晚到/重复回执不计入日增量，以及失败查询访问历史继承。

### v5 启动与队列接续

当前运行 `million-yield-v5-20260915`，独立 24 小时窗口与上一阶段最终数据见 `research/million-v4-to-v5-transition.json`。29 个源码文件 hash 校验一致。v4 最终有效新正文 6,357、Whale 新增接收 5,124、缺日期 1,233、pending 0，短阶段中止，不能作为日增成绩。

13:30:29（UTC+8）将既有实验中 1,328 个尚未抓正文的 Google 发现链接接续入队；保留原发现时间、查询、位置记录，已抓正文和历史基线 URL 不重复导入。不复制任何搜索请求统计、正文或接收回执；必须在本轮实际抓取、通过质量/去重并由 Whale 新增接收后才计数。操作脚本 `scripts/inherit_pending_frontier.py` 的隔离 SQLite 验证通过（幂等、排除基线、原查询溯源、没有虚增搜索/正文/回执、截止时间不变）。审计：`research/million-yield-v5-20260915-pending-inheritance.json`。

分钟监控独立运行，见 `research/million-yield-v5-20260915-monitor.log`。本轮继续保持 active；短时速率即使超过 694.44 条/分钟，也不是完整 24 小时百万条验收结果。

#### v5 运行中资源与索引记录

13:32:01（UTC+8）请求将正文 RSS 回收阈值从 128 调至 160 MiB，40 个槽位保持不变，流水线在下一分钟采样自动生效并记录 `body_resource_change_*`。原因是 128 MiB 导致频繁重建，观察到 CPU 约 10.4 核、内存仅 3.8 GiB；调整后一次采样为 3.66 核/4.73 GiB、最近一分钟 707 条新增接收，随后一次为 5.98 核/5.31 GiB、最近一分钟 765 条。仍需稳定区间和完整日窗口验证，不能用分钟峰值宣称达标。

13:34:37（UTC+8）在线增加 `outbox_status_due` 与 `outbox_status_finished` 两个索引，不修改任何正文、质量、状态、回执或时间窗口。100,000 已完成回执+20 待上传的合成查询基准前后返回结果一致；待上传查找从约 3.86 ms 降到 0.011 ms。只是大账本查询的局部优化，见 `research/million-v5-outbox-microbenchmark.json`。运行源码仍使用启动快照，索引变更另存 `operator_index_change_*` 审计记录；工作区 schema 同步供下一次启动使用。

独立监控增加 `scripts/million_health.py`，每分钟记录精确最近 60 秒/300 秒新增 accepted 回执、正文积压、实际生效的进程配置、CPU/内存和磁盘余量，保存到 `research/million-yield-v5-20260915-health.jsonl`。未满 5 分钟时该脚本的 5 分钟均速包含启动前空白时间，不能作为稳定区间。


## 第五轮：v6 准备、真实容量阻塞与下一步

截至 2026-09-15 14:36（UTC+8），v5 运行约 68 分钟，实际有效新正文 22,303、Whale 新增接收 18,523、缺日期 3,780、待上传 0；Google 实际请求 7,209、验证码 137。最近五分钟增量已接近零。这不是完整 24 小时结果，百万目标未达成；v5 仍在运行，源码快照未被替换。

### 已完成但尚未部署的 v6

- 正文元数据按需解析：流水线启用 `lean_metadata`，保留原正文参数及 Trafilatura 标题优先级，跳过未使用的作者/分类等字段；JSON-LD 和发布时间解析使用 lxml，异常时回退原路径。20 个真实公开页面的标题、正文、日期全部一致；交替顺序三轮对照的累计时间 1.9931 秒 → 1.5566 秒，约 1.28 倍。只代表样本中的解析性能，不能视为全链路提速。证据：`research/million-v6-real-parser-counterbalanced.json`。
- 可选 `dense` 计划：历史合格站点、合并 AI 关键词、96 个完整月份按需生成；返回满页的站点查询按日期拆分，子区间重叠一天，持久化防重复拆分。正文日期仍须原站明确声明。新增查询不等于新增文章，真实对照尚不足以证明 OR 查询增产。
- 正文调度候选窗口扩大到 2,048，避免忙碌站点挡住其他可执行任务；搜索线程可配置 1–24，并支持有审计的运行中调整。通道可显式选择，默认仍为原三个。
- Google 私有/公共端点按主机共享间隔。PostgreSQL 保存历史端点别名，组级预约读取所有别名的最长冷却和最近使用时间；缓存刷新删掉旧端点，也不丢失历史限制。预约写回旧端点，保留与旧预约逻辑的兼容。主机组只是保守限速单位，未推定不同主机必有不同公网出口。
- 本地 Google 冷却同样跨协议共享；较短错误不缩短既有冷却，迟到的成功结果也不解除尚未到期的冷却。
- 新增显式 `public_google` 测试配置，保留 HTTPS、无威胁标记和检测时间不超过 30 分钟的条件，去掉未能预测 Google 可用性的 quality/streak/latency 组合；原 `public` 配置不变。新增配置每五分钟检查是否需要同步，仍验证缓存新鲜度。

最新完整验证：145 项 Docker 测试全部通过，包括并发组预约唯一性、跨协议限速、继承历史 CAPTCHA、刷新后不遗忘旧别名、迟到成功不解封和原有质量/回执审计。本机同 145 项中 130 通过、15 数据库测试跳过。此次 Docker 数据库用例使用现有数据库中的随机测试键并清理；没有调用真实 Whale。`git diff --check` 通过。部署前仍须完成真实供给验证，不能沿用单元测试声称百万吞吐。

### 现有代理资源的实测

原公共筛选组合返回零，但不是没有公共库存。不同时间采样的未筛选库存约 1,962–1,992 条；单独 HTTPS + 无威胁标记 + 30 分钟新鲜条件一度匹配 169 条。字段筛选证据：`research/million-v6-public-proxy-filters.json`。库存持续变化，不应将不同采样时间的数量当作同一快照。

第一次真正 Google 公共池诊断同步到 142 个端点、125 台主机；24 次实际请求耗时约 122 秒，2 次成功、19 次超时、2 次连接错误、1 次 CAPTCHA，共取得 11 个不同 URL。两次成功均为 53AI 的合并关键词查询；只证明该语法能够返回结果。诊断共享原生产配额，失败照常记录并冷却，没有正文和 Whale 回执，不计入正式实验。证据：`research/million-v6-public-google-pilot.json`。

随后只选最近真正成功的两个端点，以 16 秒调度间隔安排 8 项普通/OR 对照：真正发出 4 次 Google 请求，2 次成功、2 次超时，其他 4 项因无可用端点而未发 HTTP 请求。取得 20 个不同 URL，仍未验收正文；普通/OR 配对均未完整成功，无法计算哪种查询更优。证据：`research/million-v6-public-dense-comparison.json`。这也说明一次成功不足以建立持续可用容量。

14:35 的按主机诊断：私有池 160 个端点对应 80 台主机，全部受冷却/间隔限制；最早一组约 16:24 可再次预约，不代表届时所有主机恢复，也不保证 Google 成功。公共池当时 136 端点/120 主机，98 台尚无预约限制，但只有两台最近 30 分钟有实际 Google 成功记录，后续又出现超时。未受限数量不等于可用数量。证据：`research/million-v6-google-host-capacity.json`。

### 百万目标的容量差额与费用待确认项

百万新增接收要求持续 694.44 条/分钟。4 RPS 的完整日预算最多 345,600 次搜索，因此平均每次须产出至少 2.894 条最终新增接收。若只有 80 台独立可用主机、每台最低间隔 30 秒，理想搜索上限进一步降至 230,400 次/日，需要每次至少 4.341 条新增接收；这还没有扣除验证码、超时或冷却。当前实际可用容量远低于上述理想条件。

为提供可审阅的下一步，已读取 [Serper 官方价格页](https://serper.dev/)（2026-09-15）：Starter 为 50 美元/50,000 次查询，Standard 为 375 美元/500,000 次，Scale 为 1,250 美元/2,500,000 次，Ultimate 为 3,750 美元/12,500,000 次，未含税；页面宣称可申请 2,500 次免费查询，但本项目没有该账户/密钥，也未核实领取条件。原始文本：`research/serper-pricing-20260915.txt`。另一供应商的尝试地址返回 404，未据此引用报价。

只作预算情景计算：若每次搜索最终接收 2 / 3 / 5 篇，每日分别需要约 500,000 / 333,334 / 200,000 次搜索。按 Starter 单价约 500 / 333.34 / 200 美元/日；较大套餐单价较低，但需更高预付款。该转化率尚未在 Serper 上验证，不能承诺购买后达到百万，正文抓取成本另计。

建议先在保持 Google Web 来源的前提下验证最多 50,000 次查询，首轮新增费用上限 50 美元，复用当前质量、历史去重和 Whale 接收漏斗，完成后根据真实转化率再决定扩大。已向用户询问是否允许新增此服务和费用；目前未购买、未注册、未接入或启用收费接口。现有运行及免费资源核查继续。目标保持 active，尚不能标记完成。


### 最新约束：仅现有资源，不使用付费方案

用户随后明确撤销付费方案。未购买、未注册、未启用 Serper；此前密钥配置请求作废。上节报价仅为已完成的调研记录，不再作为执行方案。

14:44（UTC+8）只读严格审计：75.87 分钟累计 18,529 条 Whale 新增接收，历史 URL/hash 重合与本轮重复计数均为零。剔除 681 条继承的待抓 URL 后，本轮 Google 新发现贡献 17,848 条；7,209 次实际请求，每请求约 2.476 条合格接收。最近 30 分钟只新增 44 条，最近 5 分钟 2 条，不能用累计均速外推 35.17 万/天宣称可持续。

当前转化率下，80 台主机每台间隔 30 秒、全天完全没有验证码/超时/冷却的条件产能约 57.04 万条/天；共享 4 RPS 若始终有足够可用出口，对应约 85.56 万条/天。两者都是条件计算，并非已经实现的日上限；实际代理正在冷却，当前不能给出经过验证的免费日最大产量。15 分钟最佳窗口为 9,647 条，也不能外推成日量。完整窗口截止 2026-09-16 13:28（UTC+8），届时按真实回执验收。

复算脚本：`scripts/estimate_google_capacity.py`；数据：`research/million-v5-capacity-estimate.json`；严格审计：`research/million-v5-current-audit.json`。脚本不写实验账本，不更新或延长实验窗口。
